{
  "name": "document-processor-lambda",
  "version": "1.0.0",
  "description": "S3-triggered text extraction for Document Search (Phase 4.3)",
  "main": "index.js",
  "scripts": {
    "build": "npm install --omit=dev && npx --yes bestzip ../lambda-deploy/lambda_function.zip index.js package.json node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.490.0",
    "mammoth": "^1.6.0",
    "pdf-parse": "^1.1.1"
  }
}
