apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-search-frontend
  labels:
    app: document-search-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: document-search-frontend
  template:
    metadata:
      labels:
        app: document-search-frontend
    spec:
      containers:
      - name: frontend
        image: document-search-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: document-search-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
